import { View } from "react-native";
import MobileNavbar from "../mobile/MobileNavbar";

export default function WebNavbar() {
    return (
        <>
            <MobileNavbar onWeb={true} />
        </>
    )
}